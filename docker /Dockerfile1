
# Usar imagem base oficial do Node.js
FROM node:18
# Definir diretório de trabalho
WORKDIR /app
# Copiar arquivos para o contêiner
COPY . .
# Instalar dependências
RUN npm install
# Expor a porta padrão do Node.js
EXPOSE 3000
# Comando para iniciar a aplicação
CMD ["npm", "start"]
